# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai>

# Accepted OS-level CVEs with no fix in Debian Bookworm.
# Mitigated by container hardening: cap-drop ALL, read-only rootfs,
# no-new-privileges, rootless mode.
# Reassess on base image upgrade to Debian Trixie or Node 24.

# zlib: integer overflow in zipOpenNewFileInZip4_64 (minizip); will_not_fix by Debian
CVE-2023-45853

# openldap: null pointer dereference in ber_memalloc_x; transitive dep via libcurl
CVE-2023-2953

# git: arbitrary code execution via crafted repository
CVE-2025-48384

# git: arbitrary file writes via crafted repository
CVE-2025-48385

# glibc: integer overflow in memalign
CVE-2026-0861

# npm transitive deps — fixes exist (minimatch 10.2.1, tar 7.5.8) but
# npm 11.10.0 (latest) hasn't updated. Can't patch in-place because npm's
# package.json references private @npmcli packages not on public registry.
# Mitigated: read-only rootfs, cap-drop ALL, no-new-privileges;
# tar/glob exploits require attacker-controlled archive or glob input.
# Reassess on next npm release.

# minimatch: ReDoS via repeated wildcards (fixed in 10.2.1)
CVE-2026-26996

# tar: hardlink target escape via symlink chain (fixed in 7.5.8)
CVE-2026-26960

# glob: command injection via malicious input (fixed in 10.5.0, 11.1.0)
CVE-2025-64756

# tar: arbitrary file overwrite and creation (fixed in 7.5.3)
CVE-2026-23745

# tar: arbitrary file overwrite via Unicode path collision (fixed in 7.5.4)
CVE-2026-23950

# tar: arbitrary file creation via hardlink path traversal (fixed in 7.5.7)
CVE-2026-24842
